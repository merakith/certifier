type Certificate @entity(immutable: true) {
  id: ID!              # tokenId as string
  owner: Bytes!        # wallet address
  ipfsURI: String!
  issuedAt: BigInt!
  blockNumber: BigInt!
  revoked: Boolean!
}